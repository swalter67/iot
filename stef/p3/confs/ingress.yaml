apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-serv
spec:
  rules:
  - host: wiilapp.127.0.0.1.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wiilapp
            port:
              number: 8888